<!DOCTYPE html>
<html lang="zh-CN">
<head>
<meta charset="utf-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<meta name="description" content="中秋节丰富多彩的民间习俗">
<title>民间习俗 — 月满中华·情系中秋</title>
<link href="/css/common.css" rel="stylesheet">
<link href="/css/animations.css" rel="stylesheet">
<link href="/css/pages.css" rel="stylesheet">
</head>
<body data-journey="customs">
<div class="page-transition active"><div class="moon-loader"></div></div>
<nav class="navbar">
  <a href="/" class="nav-logo"><div class="logo-icon"></div><span>月满中华</span></a>
  <div class="nav-links">
    <% nav.forEach(function(item) { %>
      <a href="<%= item.href %>"<%= item.href === '/customs' ? ' class="active"' : '' %>><%= item.label %></a>
    <% }); %>
  </div>
  <div class="nav-toggle"><span></span><span></span><span></span></div>
</nav>
<div class="bg-particles"></div>

<section class="page-hero">
  <div class="cloud-pattern"></div>
  <div class="hero-content">
    <span class="hero-icon">🎑</span>
    <h1>民俗万象</h1>
    <p class="hero-desc">民间习俗 · 丰富多彩</p>
  </div>
</section>

<div class="page-content">
  <div class="section-title reveal">
    <h2>中秋传统习俗</h2>
    <p class="subtitle">鼠标悬停卡片，了解更多民俗文化</p>
    <div class="divider"><span class="dot"></span></div>
  </div>

  <!-- Flip Card Grid -->
  <div class="flip-cards-grid">
    <% customs.forEach(function(c) { %>
      <div class="flip-card reveal">
        <div class="flip-card-inner">
          <div class="flip-card-front">
            <span class="fc-icon"><%= c.icon %></span>
            <span class="fc-title"><%= c.name %></span>
          </div>
          <div class="flip-card-back">
            <p class="fc-desc"><%= c.card_desc %></p>
            <span class="fc-more">—— 悬停翻转 ——</span>
          </div>
        </div>
      </div>
    <% }); %>
  </div>

  <!-- Category Detail Sections -->
  <% categories.forEach(function(cat) { %>
    <section class="customs-detail-category" id="<%= cat.anchor %>">
      <div class="cat-badge reveal"><%= cat.name %></div>
      <h2 class="reveal"><%= cat.name %></h2>
      <p class="subtitle reveal"><%= cat.subtitle %></p>
      <div class="divider reveal"><span class="dot"></span></div>

      <% cat.customs.forEach(function(c) { %>
        <div class="cdc-card reveal">
          <div class="cdc-header">
            <span class="cdc-icon"><%= c.icon %></span>
            <h3><%= c.name %></h3>
            <span class="cdc-badge"><%= c.badge %></span>
          </div>
          <div class="cdc-tabs">
            <% c.details.forEach(function(d) { %>
              <button class="cdc-tab" data-cdc="<%= c.anchor %>" data-tab="<%= d.tab_key %>"><%= d.tab_label %></button>
            <% }); %>
          </div>
          <div class="cdc-panels">
            <% c.details.forEach(function(d) { %>
              <div class="cdc-panel" data-cdc="<%= c.anchor %>" data-panel="<%= d.tab_key %>">
                <%- d.content %>
              </div>
            <% }); %>
          </div>
        </div>
      <% }); %>
    </section>
  <% }); %>
</div>

<footer class="footer">
  <div class="footer-content">
    <div class="footer-links">
      <% nav.forEach(function(item) { %><a href="<%= item.href %>"><%= item.label %></a><% }); %>
    </div>
    <div class="footer-divider"></div>
    <div class="footer-info">
      <p><span class="highlight"><%= config.site_title %></span></p>
      <p><%= config.footer_text %></p>
    </div>
  </div>
</footer>
<div class="back-to-top" title="回到顶部">
  <svg viewBox="0 0 24 24"><path d="M12 19V5M5 12l7-7 7 7" stroke="currentColor" fill="none" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/></svg>
</div>
<script src="/js/common.js"></script>
<script src="/js/moon-journey.js"></script>
<script>MoonJourney.start('customs');</script>
<script>
// Tab switching logic
document.querySelectorAll('.cdc-tab').forEach(function(btn) {
  btn.addEventListener('click', function() {
    var cdc = this.dataset.cdc;
    var tab = this.dataset.tab;
    // Update active button
    this.parentElement.querySelectorAll('.cdc-tab').forEach(function(b) { b.classList.remove('active'); });
    this.classList.add('active');
    // Update active panel
    var card = this.closest('.cdc-card');
    card.querySelectorAll('.cdc-panel').forEach(function(p) { p.classList.remove('active'); });
    card.querySelector('.cdc-panel[data-panel="' + tab + '"]').classList.add('active');
  });
});
// Initialize first tab of each card as active
document.querySelectorAll('.cdc-card').forEach(function(card) {
  card.querySelector('.cdc-tab').classList.add('active');
  card.querySelector('.cdc-panel').classList.add('active');
});
</script>
</body>
</html>
